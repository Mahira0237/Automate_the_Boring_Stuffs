import re

regexspace=re.compile(r'\s(.*?)\s+|(.*?)\s+|\s(.*?)')
string=' hsvcsh nsxjnsj sjnjcs '
gotSpace=regexspace.search(string)
if gotSpace is not None:
    arg=gotSpace.group(0)
    print(arg.replace(" ",""))
else: print(string)

